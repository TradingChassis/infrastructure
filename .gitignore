# OS
.DS_Store

# Env / secrets
.env
*.env
*.pem
*.key
*.crt

# Operator-local OCI API signing material lives under $HOME/.oci (outside Git).
# *.pem already covers typical API-key filenames such as tradingchassis_api_key.pem.

# Kubernetes
.kube/
*.log

# Terraform
terraform.tfstate*
*.tfvars
*.tfplan

# Operator-local OCI remote-state backend configuration (keep example tracked)
terraform/backend.hcl

# Helm
charts/
*.tgz

# IDE
.vscode/
.idea/

# Temporary
tmp/

# --- Version 2 local artifacts (keep tracked examples usable) ---

# Terraform local plugin/cache directory (keep .terraform.lock.hcl tracked)
.terraform/
crash.log
crash.*.log

# Operator-local Ansible inventory and private-runtime extra vars (keep examples tracked)
ansible/inventory/local.yml
ansible/extra-vars/private-runtime.yml

# Kubernetes local kubeconfig filenames
kubeconfig
kubeconfig.*
*.kubeconfig

# Certificate and SSH private-key filenames
*.p12
*.ppk
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*
id_ecdsa
id_ecdsa.*
id_dsa
id_dsa.*

# Local Python virtual environments
.venv/
venv/

# Ansible Vault password files
*.vault_pass
*.vault-pass
.vault_pass
.vault-pass
vault_password
vault_password.txt

# Rendered secret output directories
rendered-secrets/
secrets-rendered/

# Local diagnostics
diagnostics/
local-diagnostics/
*.runtime.log
*.diag.log
