---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Prepared V2 tree: workloads only. SecretProviderClass resources are owned by
# Ansible private deployment configuration after live cutover and are intentionally
# omitted here. This overlay is not active yet.
resources:
  - ../../base

patches:
  - path: aws-default-region-patch.yaml
