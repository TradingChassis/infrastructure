---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Active V2 tree: workloads only. SecretProviderClass resources are owned by
# Ansible private_runtime_config and are intentionally omitted here.
resources:
  - ../../base

patches:
  - path: aws-default-region-patch.yaml
