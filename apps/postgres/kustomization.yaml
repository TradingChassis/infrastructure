---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Active Argo CD entry point for Greenfield V2.
# Workloads only; SecretProviderClass resources are owned by Ansible
# private_runtime_config (not Git).
resources:
  - overlays/v2
