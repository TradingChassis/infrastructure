---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Active Argo CD entry point. Keep pointing at the V1-compatible overlay until
# the private runtime-configuration cutover is explicitly authorized.
resources:
  - overlays/v1
