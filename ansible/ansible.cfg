[defaults]
# Paths in this file are resolved relative to this ansible.cfg location.
inventory = inventory/example.yml

# Foundation defaults intentionally avoid privilege escalation and SSH identity.
# Live host connection settings are defined in a later execution scope.
host_key_checking = True
gathering = explicit
retry_files_enabled = False
interpreter_python = auto_silent
